(()=>{const defs=[['showReviewResult','แสดงผลการตรวจ','ผ่าน ไม่ผ่าน หรือกำลังตรวจ'],['showRejectionReason','แสดงเหตุผลที่ไม่อนุมัติ','เหตุผลจากหัวหน้างาน'],['showPaymentStatus','แสดงสถานะการจ่าย','รอจ่าย อนุมัติจ่าย หรือจ่ายแล้ว'],['showReviewerNote','แสดงหมายเหตุหัวหน้างาน','ข้อความสำหรับผู้ปฏิบัติงาน'],['showStatusHistory','แสดงประวัติสถานะ','ลำดับเหตุการณ์ของงาน'],['showReviewerIdentity','แสดงชื่อและวันที่ตรวจ','ข้อมูลผู้ตรวจ'],['showPaymentAmount','แสดงยอดเงิน','จำนวนเงินที่เกี่ยวข้อง']];
async function load(){try{const me=await Api.request('/api/staff/me');if(me.user.role_code!=='ADMIN')throw new Error('ไม่มีสิทธิ์ Admin');const d=await Api.request('/api/admin/settings/field-visibility');document.getElementById('settings').innerHTML=defs.map(([k,n,x])=>`<label class="row"><span><b>${n}</b><small>${x}</small></span><input type="checkbox" data-key="${k}" ${d.settings[k]?'checked':''}></label>`).join('')}catch(e){await UI.error('เปิดหน้าตั้งค่าไม่ได้',e.message);location.href='admin-login.html'}}
document.getElementById('saveBtn').onclick=async()=>{const p={};document.querySelectorAll('[data-key]').forEach(x=>p[x.dataset.key]=x.checked);try{UI.loading('กำลังบันทึก...');const d=await Api.request('/api/admin/settings/field-visibility',{method:'POST',body:JSON.stringify(p)});UI.close();UI.success('บันทึกสำเร็จ',d.message)}catch(e){UI.close();UI.error('บันทึกไม่สำเร็จ',e.message)}};load()})();