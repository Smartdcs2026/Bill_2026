(()=>{const $=id=>document.getElementById(id);let months={};function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function load(month=''){try{const d=await Api.request('/api/field/assignments'+(month?`?month=${month}`:''));months=d;$('assignmentDate').type='month';$('assignmentDate').value=d.month;$('assignmentDate').min=d.previousMonth;$('assignmentDate').max=d.currentMonth;$('assignmentStatus').textContent=`งานเดือน ${d.month} จำนวน ${d.assignments.length} ร้าน`;$('assignmentList').innerHTML=d.assignments.map(a=>{const s=a.store||{},locked=d.readOnlyStatuses.includes(a.status);return `<article class="panel"><div class="muted">${esc(a.work_date)} · ${esc(a.status)} ${locked?'· ล็อกแล้ว':''}</div><h3>${esc(s.brand)} — ${esc(s.storeName)}</h3><p>${esc(s.storeCode)} · POS ${esc(s.posCount)}</p><p>${esc(s.address||'')}</p><button class="btn btn-primary">${locked?'ดูข้อมูล':'เปิดทำงาน'}</button></article>`}).join('')||'<section class="panel">ไม่มีงานในเดือนนี้</section>'}catch(e){UI.error('โหลดงานไม่สำเร็จ',e.message)}}
$('loadAssignments').onclick=()=>load($('assignmentDate').value);load();
})();